# Adapter — Cursor

**Active file:** root `.cursorrules` (routes Cursor to root `SKILL.md`).

Modern Cursor also supports `.cursor/rules/*.mdc` (frontmatter-scoped). To migrate,
create `.cursor/rules/secure-planning.mdc` with the same routing instruction.
