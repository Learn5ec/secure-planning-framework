# Adapter — Windsurf

**Active file:** root `.windsurfrules` (routes Windsurf to root `SKILL.md`).

Windsurf also supports `.windsurf/rules/*.md` for multiple scoped rule files;
`.windsurfrules` is the single-file equivalent used here.
