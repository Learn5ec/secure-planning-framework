# Adapter — VS Code (GitHub Copilot)

**Active file:** `.github/copilot-instructions.md` (routes Copilot to root `SKILL.md`).

Copilot reads this automatically for repo-aware chat. Keep it in sync with
`.cursorrules` / `.windsurfrules`.
